# ✅ render.yaml (must start with this!)
type: blueprint
services:
  - type: web
    name: blast-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    pythonVersion: 3.10
